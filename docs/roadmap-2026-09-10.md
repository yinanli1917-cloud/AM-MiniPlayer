# nanoPod 路线图 2026-09-10（待创始人确认）

状态：草案。创始人确认后逐项开工；带【待裁】的点在到达时单条请示。

## 0. 六月到九月现状

| 月份 | 提交数 | 主线 |
|---|---|---|
| 2026-06 | 160 | 原生 CALayer 歌词引擎重建：切行、模糊、间奏点、隐式动画根治（postmortem 009）；管线九轮 review（源注册表、显示状态机、负缓存、单飞、预热、未命中备忘） |
| 2026-07 | 50 | 手感回收（切行、暗底连续、dots 落行）；身份纪律（PID 三门）；电台匹配；翻译单发布；性能（loop 停摆、模糊经济、玻璃底材 A/B） |
| 2026-08 | 41 | T0 收编搁浅分支；T1 队列可行性与伴生定位；T2 三 bug + 磁盘预检 + 3s 预算 P1（发布后冻结）；崩溃修复；压测三层 + e2e 冒烟；A 规则（原文 ≤3s）；逐字优先回归；v0.29 |
| 2026-09 | 2 | 规矩单源化文档 |

- 发售线 v0.29（08-27）。测试函数 1016 个（`grep func test`）。GitHub 无开放 issue。
- 已拍板不做：编辑 Up Next（路线 A/B 均否，08-25）；替代原生播放器；菜单栏歌词；$0.99 收费。
- 已拍板口径：原文歌词全路径 ≤3s，翻译并行后补不闸原文（A 规则，08-26）。这条已经替 3s 预算方案的「审点④翻译取舍」做了裁决。
- 仓库杂项：`cursor-nanopod-closeout-2026-08-26.md` 已过时（a07fc8a 已在 main）；`nanopod-tonight` worktree 路径失效可 prune；未跟踪 `.agents/ .trae/ research/ docs/搬迁残留-2026-09-09/`；`NOTES.md` 一行路径修正未提交。

## 1. 想法来源

- ChatGPT「Define KyB Subjectivity」2026-09-03：队列顺序高频但不替代原生；设置页难看要重构并加情怀小动画（Pinterest「ani」文件夹）；支持 Spotify/网易云/QQ 音乐/YouTube 等所有 macOS 播放器；对标 Liqoria 的丝滑与玻璃，否定灵动岛与菜单栏歌词；古典乐 guidance；作品集网页（另项目）。
- Apple Notes「nanoPod待办」2026-01-08：菜单栏设置图标、引导页、快捷键映射、网易云/QQ 支持、Liquid Glass 适配（已做）、波浪动画（已做）。
- 仓库文档：status-08-21、t0/t1/t2 系列、crash-08-25、lyrics-ux-contract、TODOS.md、research/references/liqoria 两份 spec、nanopod-defects-07-27 录屏 spec。
- 今日新增（创始人 09-10）：贴边隐藏要有 Liquid Glass 水吸附的液态感，像灵动岛那样贴边；页面切换与一切可加微交互处全部加上。

## 2. 任务清单（按 worktree 分组；组内顺序做，组间并行）

### WT-A 歌词管线：缓存与匹配
- A1 apply-on-select：结果 2.0s 就绪、4.2s 才上屏，withHardTimeout 续体被排水拆卸饿死；选中即直跳 MainActor apply。【09-11 WT-A 已完成于分支：裁决在 withTaskGroup 闭包内即 deliver，排水留后台；复现 gap 0.45s→<1ms，7 条测试；09-11 已合入 main a693c8e。后续项：LyricsVerifier 在 release 配置编不过（用了 DEBUG-only cachePolicy 入口）】
- A2 负证据缓存 + 英文标题门：纯英文歌冷取每次 10-20 次 iTunes/MusicKit 白费调用；Branch-2/3 复用 isLikelyEnglishTitle；「成功才落盘」改为负结果也落盘（带 ledger 配额）。
- A3 内存缓存扩容：LyricsService 内存缓存仅 50 条，命中率极低；改按 cost 的 LRU。
- A4 3s 预算 P2：backfill 折进单个 ≤3s 并行窗，一次裁决一次发布，翻译并行。前置：先用 DEBUG 埋点采一周「backfill HIT 而前台 miss」真实分布【待裁：是否先埋点采数再改】。【09-12 主会话按创始人 08-27 先复现再修规则定：先埋点采数；474ca83 埋点已合入 main 5c619c7，每次取词写一行 ~/Library/Application Support/nanoPod/lyrics-backfill-census.jsonl，采一周后发主会话再定 P2 改法】
- A5 翻译持久化与分块：翻译落盘（song+lang），分块增量 + 超时，不再每次重建 TranslationSession，去掉 0.6-1s 人为延迟地板。【09-12 已合入 main 5c619c7：6b34b7f 落盘/分块/可用性记忆化/去 0.55s 延迟，4e3b59a 跨曲复用一个 TranslationSession + 请求合并；另 258df88 重试也清负证据行】
- A6 匹配开放缺口三条：CN 罗马字仅艺术家解析替换艺术家身份需同等印证；LRC「歌手 - 歌名」头行污染剥离；片假名外来语标题英→片假名罗马字通道。【09-12 已合入 main 5c619c7：a0397e5 三条全部关闭，Dinner/Kay Huang 用例随之修好。WT-A A1–A6 全部落地；A7 等录屏，A8 后期。后续项：LyricsVerifier release 编译、Wrong Love fixture 允许源加 AMLL、二十岁的浪漫用例待真实时长、NetEase YRC 词文本多于行文本（WT-B 发现）】
- A7 Bug#2 首字空白：阻塞，需创始人给歌名或录屏（哪一行、是否强调词、何时）。
- A8（后期）T4：按 persistentID 走 MusicKit 目录查询取原生 CJK 标题，罗马音机器降为回退。依赖 developer token 签名。

### WT-B 歌词渲染：行间距与切行动画
- B1 行距漂移（80c5a7e 已复现，2 条 XCTExpectFailure 在案）：非活跃行模糊足迹随距离变化撑开行距。先决定目标形态再修【待裁：行距是否随模糊变化】。
- B2 07-27 录屏症状 2：退场行与入场行动画时钟不同步，入场滞后约 100ms、越下越多。先用 NativeLyricsHandoffClockTests 的确定性时钟复现，确认是否已被 08-27 的 damping 20 对齐覆盖。
- B3 07-27 录屏症状 1：某激活行亮度封顶 162。仅相关性观察，先复现，复现不了加 DEBUG 埋点。
- B4 间奏/前奏点结构统一：点=真正的行，删 overlay/blend/interludeAfterIndex 特判；前奏点坐焦点中心当当前行。动 segmenter 与 index 映射，风险高，单独分支。
- B5 性能收口：模糊经济 Gate#4 安静桌面复测；Gate#5 暂停虚位面板残留 7.9 CPU 做一次进程 sample；CPU-idle 门实机 A/B（ps 采样）。【09-11 首轮：Gate5 硬证据 PASS（暂停面板 5s sample 内零 commit/零 displaylink/零 tick）；Gate4 与 idle 门因 load 2.3→5.2、WindowServer 本底 45% 不可定论，需真正安静时段 30s×3 复测。09-12 15:25–15:39 安静复测（swift 进程 0）：Gate4 PASS——葉子逐字歌 A-B-A 三轮，光栅化 OFF 比 ON 贵 WindowServer 中位 +13.2 点，方向一致，ON 臂三轮内差 <1 点；idle 门与 Gate5 未跑（时间不够）。另发现 release 下 enableDebugFileLog 开关可能无效，核实中】【09-13 静止窗（四会话冻结、0 编译进程）：Gate5 PASS——暂停歌词页对空窗 WS −1.45 点（门 ≤+5）、暂停 app 0.1%（门 ≤0.5%）、5s sample 零 commit/零 displaylink/零 tick；idle 门 PASS——Fureai 行级+译文三窗 app 中位 0.6/0.5/0.5%（门 ≤1.5%）。B5 三门齐全收口。debug 开关结论：域名形式 defaults 被残留沙盒容器劫持，须写显式 plist 路径】
- B6 切行手感终验：创始人实机看一次（T0 遗留）。

### WT-C Liquid Glass、液态动效与微交互
- C1 贴边隐藏液态化：【09-12 创始人定 (a)：保留吸附引擎与动效，内容层加玻璃 morph，「先尝试吧」；同时要一份代码层面的 Liquid Glass 原生度审计】现有 hideToEdge / 6pt 露边 / hover 探出 30pt / 点击恢复保留，加内容形态 morph（card↔pill）与材质渐变。参照 Liqoria 三阶段不同步时序：几何 125-150ms、内容解析滞后 20-80ms、材质 settle 270-350ms、目标元素提前约 20ms 预播种、圆角全程连续。玻璃臂（NSGlassEffectView）只套在轻内容 pill 上，密内容 card 保持不透明。先出设计稿再写码。
- 原生度审计 09-12（research/liquid-glass-native-audit-2026-09-12.md）：12 元素 3 已原生 / 6 官方之上自绘有理由 / 3 无理由自绘或空白（页签栏死代码、音量无 UI、窗口显隐硬切已修）；优先五项：窗口显隐（已做）、真实页签栏、Reduce Transparency 全仓零响应、进度条隐式动画防护核实、滚动渐进模糊换官方 scrollEdgeEffectStyle。
- C2 页面切换液态化：MiniPlayerView 统一的 spring(0.25, 0.9) 拆成几何/内容/材质三条时钟；matchedGeometryEffect 保留。
- C3 微交互全清单：【09-12 六项已在分支：hover 胶囊、按压缩放、进度条 hover、Shuffle/Repeat 弹簧、窗口淡入淡出、注册表；对照臂 nanopod://debug/feel/<channel>/<arm>；09-12 已合入 main 23ff8b4，连同 C1 第一个 commit（EdgePresentation 状态机 + SnappablePanel 回调 + Reduce Motion 瞬切）】hover 高亮胶囊（200-300ms）、按压缩放、进度条 hover、Shuffle/Repeat、音量、收藏、页签、菜单栏图标点击。先列清单再逐个做，每个都有对照臂。
- C4 设置页重构：视觉重做、Tab 切换转场、Toggle/Picker 反馈动画、背景情怀小动画（音符等，需创始人给 Pinterest「ani」参考图）、菜单栏设置入口图标。
- C5 浅色封面对比度（TODOS P1）：Apple Music iOS 方案封面 blur+压暗+饱和替代系统材质；需实时调参，创始人终验。
- C6 引导页 Onboarding：首次启动介绍功能与授权（MusicKit、自动化权限）。
- 全组手感终验归创始人。

### WT-D 队列与伴生操作
- D1 jump-to 可发现性与反馈打磨。【09-11 已合入 main 41de5cd：点行等待态 + hover 手形，手感待创始人终验】
- D2 Love/收藏：列表行滑动或长按。
- D3 Up Next/Recent 空态文案：电台/AM 流下明说无队列信息。【09-11 已合入 main】
- 09-12 来源矩阵（docs/wt-d-queue-source-matrix-2026-09-12.md）：库歌单与已入库的 AM 订阅歌单可读队列；AM 目录单曲/电台无队列对象（SB 返回未解析代理，读出 0 首→曾误显「Queue is empty」，修法：读代理 name 为 nil 即判来源无队列——09-12 已合入 main 998c8c2，classifyPlaylistProxy 纯函数 + 9 条测试）。页面形态 A/B/C 待创始人裁。
- D4 队列实时同步收尾：读 current playlist 的漂移与刷新时机核实（08-21 标「进行中」）。【09-11 已合入 main：SB 超时不清行、Music 退出清幽灵行、Recent 扫描 generation 守卫；后续项：库歌单→电台上下文失效纯函数、纯重排 hash 探测】
- D5 全局快捷键映射：播放/暂停、上下首、显示/隐藏面板、贴边隐藏。
- 不做：编辑 Up Next。09-11 补证据：ScriptingBridge 上 move track 在正在播放的库歌单实测顺序不变（路线 A 连窄口子也死）；MusicKit SystemMusicPlayer 平台清单无原生 macOS；MusicPlayer.Queue 只有 insert afterCurrentEntry/tail，无 remove 无重排。遥控 Music.app 的任何公开路径都无完整重排。09-11 更正：nanoPod 自己当播放器（ApplicationMusicPlayer，路线 B）时 queue.entries 为 get set 且遵循 RangeReplaceableCollection，可任意重排（Liqoria 即此法），但与 Music.app 的 Up Next 无关、需订阅与 Developer Program。重排按钮做不做 = 是否重开路线 B。09-11 创始人裁定：坚持 B，不重开，重排不做。创始人同时提出歌单页因此显得无用、重排仍属较高频，待阶段包终验后再议歌单页定位。

### WT-E 第三方播放器支持（先 spike）
- 09-11 创始人定：产品分两个版本——纯净版上 App Store；完整版不上 App Store，支持网易云、QQ 音乐等多端（首次记载，无更早记录）。09-12 创始人定稿：完整版为 GitHub 开源版，「无论合规规则」实现全部功能 → 完整版豁免私有 API 与沙盒约束，按 target 隔离；纯净版不含。创始人称以前实验过系统能取网易云/QQ 官方客户端播放状态，无记录可查，待 WT-E 静态检查与创始人补充。
- E1 播放源抽象：协议化 now-playing、控制、队列读，Apple Music 为第一实现。【09-11 第一步已合入 main 02f9e93：PlaybackSource 协议家族 + AppleMusicPlaybackSource + Registry，未接 UI；第二步（MusicController 从当前源发布）随 E3 一起做】
- E2 可行性 spike：Spotify 有 AppleScript 字典，最易；网易云/QQ 音乐/YouTube 的 macOS 客户端无公开脚本接口，规矩禁私有 API，需查 Accessibility/浏览器脚本等公开路径。spike 结论决定 E3 范围【待裁：只做 Spotify 还是继续】。
- E3 按 spike 结论实现。【09-12：完整版方案 docs/wt-e-full-edition-plan-2026-09-12.md 审过，定 SwiftPM 产品切分（A）；Spotify 创始人定做，免费账户「有限先尝试」，主会话负责安装，登录归创始人。09-12 已装 Spotify 1.2.99.317（brew cask），未启动未登录】

### 主 checkout 杂务（不进 worktree）
- H1 删过时 closeout 文件；prune 失效 worktree；决定 `.agents/ .trae/ research/ 搬迁残留/` 入库或加 gitignore；提交 NOTES.md 路径修正。
- H2 CLAUDE.md 测试数更新为 1016；新增本路线图与 docs 索引。
- H3 电台实机验证（07-19 遗留 PENDING）：创始人播一段电台，看 RADIO CANDIDATE 日志。
- H4 App Store 沙盒风险项（WT-D 09-10 验证顺带发现）：沙盒下用 ScriptingBridge 控 Music.app 依赖 apple-events temporary-exception，2025–2026 无一手过审证据；上架前需单独验证，见 docs/apple_music_access_compliance.md。

## 3. 排期建议（四周，每周末 kyb 里程碑一行）

| 周 | WT-A | WT-B | WT-C | WT-D | WT-E |
|---|---|---|---|---|---|
| W1 09-11~09-17 | A1 A2 A3 | B1 B2 复现与裁决 | C1 设计稿 + C3 清单 | D1 D2 D3 | E1 E2 spike |
| W2 09-18~09-24 | A4 埋点采数 + A5 | B2 B3 修 + B5 | C1 实现 + C2 | D4 D5 | E 裁决 |
| W3 09-25~10-01 | A4 实现 + A6 | B4 | C3 + C4 | 收口 | E3 |
| W4 10-02~10-08 | A7 A8 视输入 | B6 终验 | C5 C6 + 终验 | | E3 |
| 收尾 | postmortem check → v0.30 发售线 | | | | |

H1-H3 在 W1 第一天做。

## 4. 分工与工作方式（创始人 09-10 定）
- 一个 worktree 一个独立会话（桌面 app 新 session）。会话内规划与验收 Fable 5.1 effort medium；写码、跑测试、搜资料由会话自己派 Sonnet 5 子代理做。
- 调研在各自会话里边做边搜，不集中到主会话。WT-C 的 Liquid Glass 调研必须走官方材料（WWDC25 Liquid Glass 系列 session、HIG Materials/Motion、glassEffect / GlassEffectContainer / glassEffectID / NSGlassEffectView 文档）加开源 Liquid Glass 动画实现，每个想要的动画先找到官方或开源对应物再动手。设计稿细节由创始人找参考后到相关页面确认。
- WT-E 不预设不可行：网易云、QQ 音乐、YouTube 已有大量做得好的第三方开源播放器工具，先研究它们怎么拿 now-playing 与控制播放，再定路线；仍禁私有 API。
- 会话之间有牵涉（同一文件、共享协议、时序依赖）用 session 间 message 直接沟通，不经创始人。
- 交付节奏：主会话按阶段收编各 worktree，出一个阶段性 binary bundle（build_app.sh，BuildInfo 列出本阶段包含的条目）。创始人一次性试完，在主会话给一次性反馈，主会话拆条转达各 worktree。
- 手感类最终验收归创始人；自验只做代码层面（单测、时间戳日志、假时钟、确定性回放）。先复现再修；不写白名单补丁；合回 main 即删 worktree 与分支。

## 5. 明确不在范围
灭动岛式多形态依附、菜单栏歌词、替代原生播放器、路线 A/B 队列编辑、$0.99 收费、作品集网页（另立项目）、古典乐 guidance（想法保留，v0.30 后）、computer-use 控制层（仅调研）。

## 5a. 阶段包 1 创始人反馈（09-12，录屏 /Users/yinanli/Movies/Omi Screen Recorder/Screen-2026-09-12-143346.mp4）
- 歌单页：非库曲播放时 Up Next「Queue is empty」，创始人判为误导无用；除非实时队列同步完全实现。→ WT-D：D3 来源门未命中此场景；给出各来源可读矩阵后定页面形态。截图另见 History 标题与「Recent tracks」空态文字重叠。
- 渲染（→ WT-B，录屏为创始人提供，允许逐帧分析）：① 很多行歌词遮罩重复/重影；② 整个遮罩全亮（点满）仍复现，录屏内有；③ 前奏三点未像第 0 行那样居中并带激活行效果（B4）；④ 滚动后位置有几像素的瞬间重排，肉眼可见不自然；⑤ 波浪与 SwiftUI 时代「向上的力逐行提起、渐进」的手感有差距，需对照 git 与本地记录。
- 性能与 Liquid Glass 动效尚未体现（WT-C 未落地，C1 待裁）。

## 5b. 出包门禁
- 每个阶段包出包前，主会话在收编后的 main 上跑 scripts/e2e_smoke.sh（真 release 包冷启动→切歌→seek→无词→退出，JSONL 断言），通过才出包。worktree 不跑它（会抢 Music.app）。


## 5c 阶段包 2 收编记录（2026-09-13）
main 72ebf7c = 5c619c7(WT-A) → cb73372(WT-B ff：ce19929 重影修复 + B5 附录) → 1308a8a(merge WT-C 1e4d7f8 C1 三 commit) → f7f287c(merge WT-E 05cdb02 双版本骨架) → 683c22f(merge WT-D 815d942 随机循环实时化 + History 标题空态) → 3084113(merge WT-C d3623f5 C4 v1) → 72ebf7c(merge WT-A 952502a 测试修) → 5e85f31(merge WT-C a8bf3c8 C2 页面切换)。阶段包 2 = 5e85f31，e2e 5/5 PASS，说明见 docs/stage-bundle-2-2026-09-13.md。
独立验证（主会话 detached 检出）：f7f287c 与 3084113 各跑两个 release 产品 + assert_pure_edition 4/4 + 全量（1245/1279 条，4 失败均 XCTExpectFailure，0 意外）；72ebf7c 跑 release + 新增测试文件 65 条 0 失败。


## 5d 09-14 创始人试阶段包 2 后：停新功能、只修 bug；阶段包 3（7659d5d）→ 3c（7072a14）→ 3d（b45ade8）→ 3e（af41700）→ 3f（1f034a8）
- 裁决：C1 贴边 morph 撤回（默认 v0，他自己改）；C4 分页动画方向反了（默认 system）；C5 对比度默认 legacy；不加新功能。
- 更正：阶段包 2 未含重影修复（09-13 ff 合并被未跟踪文件静默拒绝，见记忆 merge_silent_abort_untracked）；09-14 补合 c66cff1。
- 修复合入：渲染（强调词重影挖空、同周期行高落地消灭滚后整叠跳、三点居中 + 手动滚回相位时钟锚定、mask trace defaults 开关、真实时钟全亮复现台）94485c7/7659d5d；管线（翻译会话不再随页面销毁、覆盖/空档比例惩罚 + 头部偏移惩罚 + 去死门槛、偏好源容忍带让位时间轴缺陷、AMLL 日志）595dd19。基准 100 首 97→97 零翻转，82 条 74→74。
- 未复现：逐字歌单行整行全亮（六项条件已排除，真机靠 NanoPodMaskTraceEnabled 留证）；seek 回 0 后三点完全不出现。
- 待裁：手动滚动期间全行 0.6/0.95 淡化设计是否豁免停留行。
- 09-15 掉帧元凶（两套二分 + 进程样本）：H 的「Up Next 条件显示」提交把当前曲放进 History，行内 waveform symbolEffect 在歌词页后面 60fps 常驻；修法 1449fd0（页面不可见禁行内持续动画 + History 不显示当前曲）：7.4→40→7.5%。副作用修复：行封面策略 a339486（SB 12→0）、Up Next 容器常驻 a775cce。重抓循环未复现→正式诊断日志 63ce52f。三点撤回水平居中 20878d0；b12db38 撤回后由 d8f45e8 重做。
- 09-17 创始人试 3c：其余有所修复；仍在四条 + 一条确定性新路径。3d 修入：回跳后遮罩消失（行后淡出地板 seek 不复位，e9e8ad3）、每次切行 1.6pt 位移（0.95 缩放 X 锚点在行框原点而非文字左边缘，74507a7）、强调词结构重做（同管线+连续加强+离线模糊位图，臂 current|v28|amll，默认 amll，3a86676/f2538d3）、CJK 旧光栅化位图与实时扫描重叠 6 帧（59647e1）、三点失同步（外部 seek 不释放手动滚动冻结，e9ed7b4）。未复现：偶发切行跳变（已加行位置埋点 c755d1c）。后续项：QQ QRC 逐字接口；悬停背景/三点容器 <1pt 残差。
- 09-18：3e 修 3d 引入的「模糊行掉下来」（快照在位置弹簧未停时重拍，f1b8d8f）；3f 修每次切行竖向 ~1pt（Y 缩放锚点行中心→首行基线，dcd7b6a）；加 nanopod://debug/rowdump 与随机 seek 模糊测试；§4 两条假说证伪记录在 research/repro-2026-09-18-lyrics-render-3d.md。未解：回跳后偶发整行全亮/无遮罩（探针盲区）、三点逐帧状态机对照、CJK 重影现场（等 rowdump）。
- 说明：docs/stage-bundle-3-2026-09-14.md（3f）。

## 6. 阶段包 1 说明清单（收编时累积，出包时写进 BuildInfo/说明）
- WT-D（已入 main）：点行等待态 + hover 手形；电台/AM 流空态文案「Music 未提供此来源的队列」；SB 超时不清行、Music 退出清幽灵行。终验项：等待态、光标。
- WT-E（已入 main）：PlaybackSource 抽象，无可见变化。
- WT-B（分支待收）：B3 遮挡恢复直达目标；波浪对照臂 nanopod://debug/feel/wave/topdown | /sync | feel/reset；运行时埋点 ActiveBrightness（B3 亮度）与 LineGaps（B1 行距），release 包默认不写调试日志，创始人需先 `defaults write com.yinanli.nanoPod enableDebugFileLog -bool YES` 并重启 nanoPod，埋点才落到 /tmp/nanopod_debug.log；grep 关键字 ActiveBrightness / LineGaps。终验项：切行波浪两臂对比、行距肉眼 + 日志。
- WT-A：A1 apply-on-select 已入 main，日志关键字「apply-on-select」；A3 内存缓存 20 MiB 已入 main，无可见变化；A2 英文标题门 + 负证据缓存已入 main，schema 9 冲缓存，首启后台预热几分钟内歌词可能略慢，负证据 24h；A4 埋点采数 + A5 翻译落盘/分块/会话复用 + A6 三条匹配缺口 + 重试清负证据已入 main 5c619c7（09-12）；实机可见：翻译分块出现、同曲二播翻译秒出、换歌无预热延迟、切语言正常、无系统弹窗。
