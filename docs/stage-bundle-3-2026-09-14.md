# 阶段包 3q（2026-09-19 20:31，main b2681fa）

- 包：仓库根目录 `nanoPod.app`，git=b2681fa，md5=0e2fc34fe4a46cd066ee90bb824a5236。上一包 3p（67db7c8）。
- **切行卡一下——根因在取证探针。** 跨切行采样主线程：mask trace 每写一条记录都同步 open/seek/write/close 文件，占呈现循环样本约 8%，切行帧记录成簇 = 主线程 I/O 突刺。已把你机器上的 NanoPodMaskTraceEnabled 关掉；代码改为内存缓冲 + 后台队列常驻句柄批量落盘，主线程只 append（1000 次 <2ms 测试钉住）。关探针后再采样：呈现循环占主线程约 6%，无单点热点；剩余是否还卡由你裁。
- **折行行第二行半亮——已修。** 根因：跨越折行边界的词 run 被完整复制到它跨的每个视觉行（连 start/endTime 一起），未唱到的第二行按同一时间窗独立算出自己的部分显现，与第一行同步亮。修法：跨行 run 的时间窗按字数比例拆到各行。红测试带数值，修后绿。
- 未做：切行帧耗时分解探针与字块池预热（等这包的新采样）。
- 门禁：17 个相关测试类全绿，串行。
- 详细：research/repro-2026-09-20-lyrics-render-3q.md。

# 阶段包 3p（2026-09-19 20:08，main 67db7c8）

- 包：仓库根目录 `nanoPod.app`，git=67db7c8，md5=bff9c1f0d65dec4b5728cfbabdfc588c。上一包 3o（15b4b55，已被本包取代）。
- **整行重影——根因钉死并修，真机复验。** 《下雨天》「点点雨似渗出眼泪」rowdump 逐字探针：字块 x 与暗底绘制 x 逐字完全相等，但**字体不同**：布局/暗底为 `.PingFangUIDisplaySC-Semibold 24`，字块 CATextLayer 为 `.AppleSystemUIFontDemi 24`（自行回退到另一 PingFang 变体，居中放进 22.85 宽框）→ 每个唱过的字亮字形与暗字形不重合，整行双边。修法：字块直接使用布局解析出的字体（resolvedGlyphFont）。真机复验：tileFont == storageFont == .PingFangUIDisplaySC-Semibold:24。测试：字体名相等 + 字块与暗底字形位图覆盖逐像素一致（撤修红、修后绿）。
- **下沉——修。** 3o 我让浮起的字在淡出后 0.35s 缓出归位，每行唱完整行可见下沉，是我的设计错误。改回 v2.8：行离开当前行那一帧字块隐藏、暗底整行同帧恢复，与该行缩小/模糊弹簧同帧，归位不可见。测试：去激活后任一帧不许出现 (−2,0) 中间值。
- 暗字与亮字同一位置一起浮、一起保持（v2.8 语义，3o 已改回）；禁令文件与契约已改正措辞。
- 折行对照：两套引擎对《啟程》49 行折行完全一致（推翻我之前「折行边界」说法）；发现含空格行行高 28、无空格行 24 的同引擎差异，记录待议。
- 强调词判定：中文不进强调词（契约、v2.8 一致），未改。
- 遮罩：行内地板恒 1（3n）；GapHandoff 重亮取舍仍待你裁。
- 门禁：15 个相关测试类全绿，串行。全量未跑。
- 详细：research/repro-2026-09-20-lyrics-render-3p.md、-3o.md。

# 阶段包 3n（2026-09-19 19:05，main 05b07a5）

- 包：仓库根目录 `nanoPod.app`，git=05b07a5，md5=f83bccae97f02642433faf6c634ceaab。上一包 3m（5caefb4）。
- **「未来的旅程」往下降——已修，真机复验。** 现场（0.2s 连拍 12 份 rowdump）：唱着时暗字块和亮字块一起浮到 −2pt；行结束后一帧字块整体隐藏、暗底整行恢复画在原位，所有唱过的字硬落 2pt。违反 v2.8 模型「dim 整行保留、只让亮层 float」。修法：暗底整行全程完整绘制、不挖空、不建暗字块；只有亮字块浮 −2pt 并保持，行结束随行末渐隐淡出（真机复验：亮字块 y 不变、透明度 1.0→0.12→隐藏，无暗字块，无 blank）。
- 逐字上浮加单调地板（时钟回退不回落）。
- **暂停连击后亮层永久消失——已修（含裁决项）。** 现场：18:10:55 连击期间位置 215→83.7，18:11:36 第 17 行 bright=0，trace 里 brightHiddenWhileSweeping 连续 8 条。机制：3k 在激活边沿复位地板，但同帧又与计划淡出值取最小；坏读数那一帧计划认为本行早已唱完，地板立刻压回 0，此后无新边沿。修法：行内任何时刻亮层必须可见，地板只在行结束后才允许下降。**代价要你裁**：旧规则「已淡出的上一行在行尾附近的时钟回退时不许重新点亮」（GapHandoff）与它在单行内部无法区分，本次取「仍在唱的行不许永久失去高亮」，旧场景（时钟抖动回退到上一行行尾之前时，上一行短暂重亮）重新打开；测试已改名标注为待你验收的取舍。
- rowdump 加 mask 层信息。
- 强调词判定未改：契约与 AMLL 都是中文不进强调词，「未来的旅程」是普通字（只浮不放大）。
- 门禁：相关测试组 100+ 用例全绿；两处预存失败经 stash 隔离确认与本次无关。
- 详细：research/repro-2026-09-19-lyrics-render-3n.md。

# 阶段包 3m（2026-09-19 18:08，main 5caefb4）

- 包：仓库根目录 `nanoPod.app`，git=5caefb4，md5=4bde984a31b57d9cdd47a2708f26ccaa。上一包 3k（86f58d2）。
- **「未来的旅程」重影——按实现方式修（结构修）。** 你 09-19 截图（暗底「的旅程」右偏半到一字、下偏约 4pt）定性：激活行此前用两套排版——暗底是系统文字层自己排一段带换行符的文本、靠透明字挖空，亮/暗字块是另一套引擎逐字定位，真机上两套折行/字距不一致就成重影。修法：暗底和字块共用同一个布局对象，激活行暗底由新层直接用这份布局绘制整行（不拆字、不浮动，不碰 08-27 禁令），挖空改成对同一份文本打透明色、不影响几何。08-27 行距测试改为更严的真实不变量（激活前后折行数、各行片段高度与位置、每字横向落点零差，且暗底与字块同一布局对象）。相关 11 个测试类全绿。真机 rowdump 已确认激活行走新层、整行文本置空。
- 遮罩新路径（一句中间频繁暂停/播放）：加了 10 次交替的红测试，两档 tick 全绿，未复现；探针留着。
- 全量测试：11 处失败，两条最可疑的（进入手动滚动时行距稳定）单独重跑各 <1s 通过，全量里跑 33s 才挂，属机器满载超时；其余为已知 flaky/Spotify 环境类。规则：以后改动只跑相关测试类，全量只在你不用机器时跑。
- builds/ 六个旧对照包已移进废纸篓。
- 详细：research/repro-2026-09-19-lyrics-render-3m.md。

# 阶段包 3k（2026-09-19 03:36，main 86f58d2）

- 包：仓库根目录 `nanoPod.app`，git=86f58d2，md5=8f7c5efec123aa157e122605e049d61b。上一包 3i（71a697a）+ 3j 测试合入（0d434da）。
- **回跳后遮罩「没有高光/整行全亮」——真机现场抓到，已修，真机复验通过。** 现场（02:21，《啟程》第 5 行）：逐字进度期望=实际，但亮层透明度全程 0、亮层隐藏。根因：每行「唱完后淡出」地板只降不升，只在视图换行或 seek 落在本行那帧复位；唱完某行→继续播→回跳到更早的行→自然播回本行时，地板仍是 0，亮层永远不出来。修法：行进入激活窗那条边、或渲染时钟早于行起点时复位地板（不按 .initialLayout 每帧复位）。红测试 4 条（NativeLyricsPostSeekReactivationMaskTests），撤修 3 红、修后绿。真机复验：唱到 64s→seek 45s→播回第 5 行，亮层 1.0、逐字正常。探针新增 brightHiddenWhileSweeping。
- 整行全白 vs 模型 0.35 的矛盾：无群组透明度逃逸、字块未被移出暗底层；dim 补偿改为每帧重算加固。rowdump 现在打印 presentation 透明度与父层。
- **「未来的旅程」尾字重影——仍未复现。** 静态排版两套引擎对 13 字行一致（高 48、第二行 24）；真机 84.7s dump 显示字块浮动 22–24、暗底 0.35；模糊副本来源不在激活行自身两层里，正在扩 rowdump 到相交行与滤镜。
- 门禁：相关测试组全绿；全量在跑（每条不再跑全量）。
- 抓法（创始人 09-19 批准）：后台 computer-use 只发滚动/点击模拟手势，不截屏分析；取证靠 trace/rowdump/日志，`open -a <仓库>/nanoPod.app "nanopod://debug/rowdump"`。
- 详细：research/repro-2026-09-19-lyrics-render-3k.md。

# 阶段包 3i（2026-09-18 16:31，main 71a697a）

- 包：仓库根目录 `nanoPod.app`，git=71a697a，纯净版，md5=8d1819f84e88ec61e290fa85ac32a197。上一包 3h（15a7b8b）。
- **从头播后三点在顶上、激活行停在曲中——已修（真机日志证据）。** 位置 83s→0 时 nanoPod 自己已判出「位置跳变」，但另两个检测器不知情：连续三次把 0 当「瞬态读数」忽略，速度差又误判成暂停，约 1 秒里渲染器拿着 83s 的时钟。修法：已确认的位置跳变直接否决「瞬态忽略」和「推断暂停」。
- **冷启动三点在顶上——已修。** 根因不是 3h 那处改动，是前奏窗内滚动目标索引和语义索引分叉的老 bug；用从头播复现并带 100/250/500ms 掉帧变体。
- **中文尾字重影——已修。** 亮层逐字排版用的换行宽度与暗底不一致，尾字落错行；rowdump 现场 + 无布局重配置复现。
- **遮罩拉满/消失——仍未复现。** 已加四个变体：掉帧、手动滚动后点行、连续回跳、同 tick 双 configure，全绿。探针 `brightUnmaskedIncomplete` 留在 trace 里。
- 门禁：swift test 1499 条，10 处失败分属 6 用例，全部在改动前基线单独跑绿或在案 flaky（机器高负载全量跑才红），与本轮改动文件无交集。
- 取证命令必须指定包：`open -a "<仓库>/nanoPod.app" "nanopod://debug/rowdump"`，否则系统可能把链接派给 builds/ 里的旧包，同时跑两个 nanoPod。
- 详细：research/repro-2026-09-18-lyrics-render-3i.md。

# 阶段包 3h（2026-09-18 03:52，main 15a7b8b）

- 包：仓库根目录 `nanoPod.app`，git=15a7b8b，纯净版，md5=ea61e9a443eb7546abc1dff14131d17d。上一包 3g（79db025）里「折行行不缩放」已撤回（c431e27），缩放不是根因。
- **切行后 1–2px（停了又挪）——根因钉死，已修。** 两路独立证据对上：真机像素比对每次切行静止后 1.5–2.0s 固定一次单帧变化（12/12）；代码层测到 shouldRasterize 恰在切行后 1.65–2.02s 才翻 true。机制：非激活行带 0.95 transform，翻成位图缓存后重采样，与之前矢量直绘亚像素配准不同。修法：位图缓存改绑「是否激活」——去激活行从边界帧起恒缓存（运动期间也不关），激活行恒直绘；删掉手动签名重拍与 isSettled/hasActiveMotion 两道延迟门。测试：12 次切行，去激活帧到 3.0s 零翻转。
- **三点不出现——已修。** 同索引冻结释放时索引相等性检查失效，且 seek 不连续标志在同一 tick 内被提前消费；加渲染器级时间戳，红→绿。
- **强调词割裂——已修一处。** 挖空按未缩放字形宽度挖，强调词亮层放大最多 10% 溢出到邻字暗底；现放大时把邻字一并纳入挖空（邻字自己的静止瓦片补位）。
- 未复现（探针留在代码里）：回跳整行全亮（新增落地帧首次挂载穷举 40 例）、切歌 bloom（前 10 帧位置连续性测试绿）；中文尾字重影的「浮动判定分歧」假说已结构性证伪，另一假说（0.95 位图与 1.0 实时叠加）随第 1 条的缓存改法一起变化，等你看。
- 门禁：swift test 1489 通过 0 失败；merge-base 断言通过。e2e 未跑。
- 详细：research/repro-2026-09-18-lyrics-render-3h.md。

# 阶段包 3g（2026-09-18 01:16，main 79db025）

- 包：仓库根目录 `nanoPod.app`，git=79db025，纯净版，md5=69db31021d9232dc90fba8c18a64a3a3（Contents/MacOS/nanoPod）。上一包 3f=1f034a8。
- 你对 3f 的六条反馈只有一条真复现真修（切行 1–2px）；其余按铁律如实记「未复现」，代码里留了新探针。逐条报告：research/repro-2026-09-18-lyrics-render-3g.md。
- **切行 1–2px——改法换了。** 3f 把纵向缩放锚点放首行基线只保住首行；《啟程》几乎全是折成两行的中文行，第二行每次切行必挪约 1.7pt，所以对你等于没修。3g：折成两行以上的行不再做 0.95↔1.0 缩放，单行行保持原样。代价：同一首歌里单行行会缩、折行行不缩。**要你一眼裁：这种不一致能不能接受？不能就改成所有行都不缩。**
- **整行全亮无遮罩——未复现。** 你日志里 710 条记录「整行高亮」标志从未翻真，是探针盲区；3g 换了判据（亮层可见 + mask 未生效 + 进度未完成）写进同一 trace 文件字段 `brightUnmaskedIncomplete`，下次撞见把 /tmp/nanopod_mask_trace.jsonl 发我。
- **「旅程」中文尾字重影——未复现。** 有一条具体嫌疑路径（dim 与 bright 字块对「该词是否浮动」判断不一致就会偏右下重影，与你截图形状吻合），加了探针 `recordWordFloatDesync`。看到重影时执行一次 `open "nanopod://debug/rowdump"`，把 /tmp/nanopod_rowdump.txt 一起发我。
- **三点——未复现你说的「不出现/第 0 行卡顶部」**，只复现出 3d 记过的 X 0.9pt 残留。
- **切歌 bloom 回归——未复现**，没有红测试就没做二分。
- **强调词——**三臂 glow 与字块位置逐帧零差，这个子指标干净；你说「以前正常」但 v0.29 你已看过有重影，所以对照包放在 builds/nanoPod-v0.28.app 与 builds/nanoPod-v2.8.app，请你各放同一首英文逐字歌看一眼强调词，告诉我哪个是「以前正常」的那版。
- 门禁：swift test 1481 通过、1 失败为 HANDOFF 已记录的预存 flaky；merge-base 断言通过。e2e 冒烟未跑（会接管 Music.app）。

# 阶段包 3f（2026-09-18，只含 bug 修复；3～3e 作废）

- 包：仓库根目录 `nanoPod.app`，git=1f034a8，纯净版。md5=22ed940c5524592d3d476d7029bee90c（Contents/MacOS/nanoPod）。
- 上一包：阶段包 2（5e85f31）。更正：阶段包 2 里其实没有重影修复——09-13 那次合并被一个未跟踪文件挡住静默中止，我误报为已合入。本包是第一次真正带上渲染修复。
- 门禁：每条分支在独立 detached 检出上跑 release 构建 + 相关测试后合入，合并后做祖先断言；真 app 冒烟 5/5 PASS（3f）。
- 撤回（按你 09-14 指令）：贴边 morph 默认回旧行为；设置页分页动画默认回系统原生；浅色封面对比度臂默认回旧行为。三者代码保留，只能用 debug 链接显式打开。

## 〇-3、3f 相对 3e 新增
- **每次切行竖向 1–2px——已修。** 根因：非激活行缩到 0.95 时纵向绕行中心缩放，40pt 行内容对称收缩，刚唱完那行文字下移约 1pt；这是当初为中文换行行距特意选的锚点，不是算错。修法：纵向锚点改为该行首行文字基线（横向仍在文字左边缘 32pt），切换时刚唱完那行的基线不动，伸缩向下延伸。不变式测试：中英各单行/换行/带翻译共 6 组首行基线位移 0.0000pt；相邻行最小间隙约 7pt 不重叠；中文行距测试保持绿。代价：换行/带翻译的多行行，第 2 行起与翻译行比以前多挪一点，等你目视。
- 附带：切行时附近行的整体重定位经探针证实精确等于行高差，那是「面板跟随当前行滚动」的设计，不是 bug；之前我猜的「布局用了缩放后行高」被证伪（日志里 40→38 是探针自己乘了缩放显示）。
- **CJK 重影现场命令**（看到重影时在终端执行一次，把当前行与上一行所有可见图层写到 /tmp/nanopod_rowdump.txt，然后把文件发我）：
  ```bash
  open "nanopod://debug/rowdump"
  ```
- 三点：冷启动与 seek 回前奏两条路径的静止终态逐字段相等；手动滚回顶只差点簇 X 0.9pt（已知残留）。你说的「不像第 0 句的感觉」逐帧状态机对照还没做，等你用新包再看一眼。
- 回跳后偶发整行全亮/无遮罩：随机 seek 模糊测试 45 次 0 违反，你 3d 的记录坐实一次「几何未就绪单帧空遮罩、下一帧自愈」；仍在找探针没覆盖的状态。

## 〇-2、3e 相对 3d 新增（你报的「每次切行一条模糊的行掉下来」）
- 已修。这是 3d 自己引入的回归：那条「模糊变化时强制重拍快照」只看透明度/缩放/模糊是否稳定，不看位置弹簧；切行时模糊阶跃当帧稳定，位置还在飞，于是对飞行中的行拍了模糊快照，快照跟着弹簧掉进终点。修法：光栅化门改读呈现引擎「是否有活动运动」，位置没停不拍。复现测试 13 处命中全落在切行边界帧，修后 0；中文残影修复未削弱；回归 305 条绿。
- 另：seek 随机模糊测试已落地（真 surface、约 200 模拟秒 45 次随机 seek，0 违反）；你的 3d 记录里坐实一次「几何未就绪导致单帧空遮罩、下一帧自愈」。你看到的「回跳后偶发整行全亮/无遮罩」现有标志量不到，仍在查；三点手感、CJK 重影、切行 1–2px 竖向偏差三条在下一包。

## 〇-1、3d 相对 3c 新增（你 09-17 的四条反馈 + 一条更正）
- **回跳后遮罩消失（任何歌、某行唱完再 seek 回去只剩浮动）——已修。** 根因：每行的「行后淡出地板」只降不升，行唱完 1.5s 后降到 0，只在换行/视图复用时复位，seek 回同一行不复位，高光层永久隐藏。修法：seek 落地的不连续信号传到行视图复位地板；第一版用了过宽的信号，被间隙切换测试炸出两个真回归后收窄到只认显式 seek 与点行跳转。验：唱过第 N 行后拖回第 N-2 行，高光遮罩应正常扫掠。
- **每次切行 1–2 像素位移——已修，根因精确。** 非激活行缩到 0.95 时横向绕行框原点缩放而不是绕文字左边缘，文字距行框左边 32pt，所以 30.4 对 32.0 永远差 1.6pt。修法：横向锚点移到文字左边缘，纵向不动；测试钉住切换前后文字左边缘位移 0.000pt。悬停背景与三点容器还有 <1pt 残差，非文字元素，另议。
- **强调词——结构重做，默认切到新实现。** 旧实现把强调字画成独立定位的第二套图层加常驻阴影当光晕，两套靠约定对齐，一帧错开就重影，阴影又是合成器画的软轮廓。新实现（对照臂 amll）：强调字复用普通字的字块、浮动、挖空管线，放大上浮叠在同一块字块上随权重连续进出，光晕是一次性离线栅格化的模糊位图、与清晰字块共用同一次位置写入（测试零容差）。CPU 对照持平（8.7 对 8.8）。切回旧实现对照：open "nanopod://debug/feel/emphasis/current"，另有 v28 臂。
- **「滋味」那类中文残影——不是强调词，已单独修。** 中文从不进强调词管线。真根因：行在模糊态被光栅化缓存了旧位图，文本相位激活后实时逐字扫描开始动，旧位图与实时内容同时可见 6 帧。修法：文本相位一激活当帧撤销光栅化，快照带模糊/几何签名，不匹配即失效；模糊经济门不倒退。
- **三点位置——失同步 bug 已修。** 进度条 seek 只更新 seek 代数，从不释放手动滚动冻结，seek 落在手势期间或 2s 宽限内被忽略，激活行卡在手势开始那一行。修法：看到新 seek 代数立刻释放冻结，激活行由播放时间决定，前奏窗内三点就是激活行。用你截图场景（滚回顶、0:00、暂停）做了红→绿。
- **偶发切行跳变（你猜时钟）——未复现。** 日志里时钟纠正与行位置样本重叠太少，注入同形状纠正在真 surface 上没复现。已加行位置变化埋点（跟 mask trace 同一开关），下次撞见就有现场。
- 更正：上一包我说「阶段包 3 的 Supernatural 无缺陷」——你实测时间轴不对且非逐字，代理复核后确认候选池里根本没有逐字版本（网易那份 29 行是行级），QQ 有逐字接口我们没接；这条作为后续项，不在本包。

### 要你一眼答的
1. 回跳后遮罩：唱过第 N 行再拖回去，高光是否正常扫掠？
2. 切行后上一行/当前行是否还挪 1–2 像素？
3. 强调词：还有没有重影？浮动手感是否回到「和整行在一起」？
4. 中文行尾字（如「滋味」）的模糊副本是否还在？
5. 播到一半滚回顶、按从头播：三点是否占激活槽位、左对齐？
6. 偶发的切行跳变若再撞见：把 /tmp/nanopod_mask_trace.jsonl 与 /tmp/nanopod_debug.log 发我。

## 〇、3c 相对 3/3b 新增（你 09-14 夜报的掉帧与闪屏）
- **掉帧元凶已钉死并修复**：两套二分互证，跳变点在 09-13 方案 H 里「Up Next 条件显示」那个提交；真机进程样本定位到歌单页「正在播放」行的波形小图标持续动画——H 之前当前曲从不出现在任何行里，H 之后它是 History 最新一条，那行在歌词页后面 60 帧每秒重画。修法：歌单页不可见时行内任何持续动画不许运行（泛化门）；History 展示层不再显示正在播的这首。数据（同一首行级歌、歌词页、30s 中位）：H 之前 7.4% → H 后 40–42% → 修后 7.5/7.6%。
- 顺带修掉两处 H 的副作用：History/Up Next 行封面不再逐行走 ScriptingBridge 扫 Music.app（12 次→0），改走三层缓存、页面可见才抓、有界并发、失败退避；Up Next 段容器恢复常驻，条件切换挪进容器内部。
- **自发重抓循环**（Supernatural 每 3–6 秒重新抓歌词、LRCLIB/QQ 来回换版本 = 你看到的「模糊歌词从上滚到下」闪屏）：三轮无人值守没复现，唯一没测到的变量是 Apple Music 流媒体目录播放。已加正式诊断日志（谁发起抓取、预抓输入表、队列刷新原因），开着调试日志时自动留证。
- 三点回到与歌词同一左边缘（你澄清后撤回了「水平居中」）。
- 说明：本机今晚测出系统自己的两个音频进程（coreaudiod、audioanalyticsd）各占满一个核，那不是我们的。

### 要你一眼答的六个问题（渲染考古，答完我再派修）
1. 唱到强调词中段时，词周围是否有一圈发光光晕、光晕里隐约有第二层轮廓？
2. 三点第一次出现是贴着面板顶部，还是和第一句歌词落地位置基本一致？
3. 手动滚动松手回弹「停」下来约 1 秒后，是否又无声挪了 1–2 像素？
4. 播放中手动滚动、点一行跳过去，那行是否只有文字轻浮动、整行仍是没在唱的暗色？
5. 快节奏逐字歌是否会有某行突然整行一起亮、随即恢复逐字扫描？（开着记录器时把 /tmp/nanopod_mask_trace.jsonl 发我）
6. 唱完切到上一行、看起来彻底停下后，约 1–3 秒是否又挪了 1–2 像素？

### 要你做的一件事（重抓循环取证）
开着调试日志，在 Apple Music 目录里点开 NewJeans《Supernatural》听 2 分钟，什么都不碰，然后把 /tmp/nanopod_debug.log 发我。

## 一、你报的 bug，逐条

### 1. 重影（强调词两层墨迹）——已修
- 复现：你确认过的两张图（词中段 Δy=3.26pt 重影，词起始 0.09pt 干净）。根因：上一轮挖空只做了普通词，强调词的字形从未从暗底挖掉。
- 修法：挖空扩到强调词（同一机制，不新建路径）。修后同词同帧 Δy=0.000pt，图 research/repro-2026-09-14-lyrics-render/defect1-emphasis-ghost-state-fixed.png。中文行距、亮度连续性等测试保持绿。
- 你怎么验：英文逐字歌，盯 2–7 字母、时长 1.5s 以上的词唱到中段，应只有一层。

### 2. 滚动/切行后整叠歌词再跳 1–2px——已修
- 复现：真 surface 130 个周期抓到 4 次整叠同步跳变，12 行静止 10 帧后同帧一起跳。根因：行的真实高度要经两次异步跳转才回到定位，落地那帧整叠一起跳。
- 修法：首次测得的行高在同一周期进入定位。反向断言 130 周期 0 次跳变。
- 你怎么验：切行后盯上一行，不该再有「以为停了又挪一下」。

### 3. 手动滚回开头三点消失 / 不居中——已修（一处待你裁）
- 复现：冷开始、seek 到 0、手动滚回顶三条路径并排，前两条一致，手动滚回三点被隐藏。根因：手动滚动冻结显示后，驱动三点动画的相位时钟仍读真实播放进度，前奏窗只有头 2 秒，算出「早该淡出」。三点横向保持与激活行文字同一左边缘（你澄清后，「居中」指的是像激活行一样左对齐；曾改成水平居中，已撤回）。
- 修法：冻结到哪一行，相位时钟锚到那一行起点（与 seek 到该行等效）。修后三条路径三点状态一致。
- 待你裁：手动滚动期间所有行统一淡到 0.6、缩 0.95 是 07-12 的既有设计，所以停留行的三点仍比冷开始暗、偏 9px。保持还是让停留行保持全亮全尺寸？
- 未复现：seek 回 0 后三点「完全不出现」——seek 路径实测与冷开始一致。

### 4. 逐字歌某一行突然整行全亮——未复现，已埋证据开关
- 试过：真实 Music.app 播 On the Beat（85 行最快逐字歌）两段共 190s，记录器只抓到第二段 4 行、0 次整行亮（覆盖不足，非证明没有）；锁步时钟驱 7 首逐字歌全部切行；真实显示链路 + 真实墙钟 4 首各 60s；25 处零间隔切行定点；行视图回收复用 20 次远距离 seek；真实显示链路 + 生产同款 ScriptingBridge 轮询抖动钟（慢读、回退压制、1–3s 前跳、暂停恢复）33 个零间隔切行种子点 11546 帧。全部 0 次。
- 你要做的：打开开关后照常听歌，再看到整行亮一下，把 /tmp/nanopod_mask_trace.jsonl 发我，最后几行就是现场。
  ```bash
  defaults write ~/Library/Preferences/com.yinanli.nanoPod.plist NanoPodMaskTraceEnabled -bool true
  ```

### 5. 翻译经常不出——已修（本轮回归）
- 复现：日志 4 小时 50 多次换歌只有 3 次翻译成功。根因：离开歌词页清掉翻译会话，回来没人重建；旧版靠换歌顺带重建自愈，上一轮翻译重构把这条自愈砍掉了。跨曲复用会话换语言不会失败，真实框架测过。
- 修法：不再清会话；回到歌词页兜底重建。真实翻译框架的集成测试钉住。
- 你怎么验：开翻译，进出歌词页、连续换歌，翻译都该在。

### 6. Supernatural 时间轴跳行——已修（旧问题）
- 复现：匹配的歌没错，网易那份逐字在 77–140s 有 63s 空洞；评分对大段空洞惩罚太软，另一条「偏好源可低 12 分仍胜」的容忍带又把它保住。
- 修法：空档按超出比例连续扣分、新增头部空档惩罚、去掉选择层死门槛；容忍带在候选带时间轴完整性惩罚时不适用。修后终态换成 37 行完整版，时间轴连续，最大间隔 11s。
- 基准：100 首 97→97，0 翻转；82 条 74→74，两条真实改善（Fureai、Hiroshima Mon Amour 以前配错现在对），Burgundy Red 压线受网络波动。

### 7. Plastic Love（eill）先错后对——已修同一根因（旧问题）
- 复现：前台 3s 内接受了从中段起录的 26 行版本，回填 5s 后换成 44 行正确版。这是覆盖门失守，不是设计。
- 修法同 6；用真实候选数据钉了测试：26 行中段版本在新惩罚下过不了前台。
- 你怎么验：这首歌一开始就应是从头开始的正确歌词，不再中途换。

### 8. 「偏向行级」——查了一半
- 逐字库加了请求/命中/未命中/耗时日志。坐实 How Sweet 社区逐字库里确实没有数据。其余要看你真机调试日志（见下）。

## 二、本包里其他已在主线的东西（上一轮合入，本包未撤）
- 歌单页方案 H：History 改真实播放记录（任何来源、随机开着也真，最多 50 条，重启保留，设置里可清）；Up Next 只在资料库歌单且随机关时显示，否则固定一行说明。
- 随机/循环状态实时跟随 Music.app。
- 首次启动引导页（三页，不抢焦点；再看 open "nanopod://debug/onboarding/show"，重置 …/reset）。
- Spotify 来源代码已入库但未接界面，无可见变化。

## 三、要你做的
1. 试包，一次性反馈。
2. 开两个开关后照常听歌：上面第 4 条的 mask trace；以及调试日志（管线排查用）：
   ```bash
   defaults write ~/Library/Preferences/com.yinanli.nanoPod.plist enableDebugFileLog -bool true
   ```
   翻译不出/错配/整行全亮任一再现时，把 /tmp/nanopod_debug.log 与 /tmp/nanopod_mask_trace.jsonl 发我。
3. 裁第 3 条里的手动滚动淡化设计。
