# WT-D 队列与伴生操作 · 任务拆分（2026-09-10）

分支 claude/exciting-mclean-ccb7e0。规划/验收 Fable 5.1；写码/测试 Sonnet 5 子代理。不做：编辑 Up Next（路线 A/B 均否）。

## 代码现状（2026-09-10 普查）
- 跳曲：`PlaylistView.swift` `PlaylistItemRowCompact` Button → `MusicController.playTrack(title:artist:album:persistentID:)`；库曲走 NSAppleScript（global userInteractive 队列），am: 曲走 MusicKit。点击后无任何「跳转中」状态，AppleScript 失败只 debugPrint 不上屏。hover 只有 0.08 白底 + play.fill 图标。
- 收藏：只有 `toggleStar()`（`MusicController+Playback.swift:874`），仅当前曲、写 SB `loved`、无 @Published 状态、UI 零调用、零测试。列表行不知道是否已收藏。
- 空态：`PlaylistView.swift:128-140/163-175` 硬编码英文 "No recent tracks" / "Queue is empty"；`MusicQueueProvenance.swift`（含 unavailableDisplayMessage）整个模型从未被构造或读取；电台/流下 `currentPlaylist` KVC 失败只返回空数组。
- 队列刷新：markQueueMayHaveChanged 8 处 + 换歌后 2s 防抖 + 30s hash 探针（name+count+currentID）+ DistributedNotification 0.5s 防抖 + 打开面板时 5s 内复用缓存；SB 读全在 scriptingBridgeQueue，无 Thread.sleep。`origin/codex/real-time-queue-sync-proof-gates` 约 20 条「Music 停止/不可用时清陈旧行」加固提交从未合入，落后 main 三个月。
- 快捷键：仓库零全局键代码，Package.swift 零依赖，未沙盒。

## 任务
| 编号 | 内容 | 依赖 | 状态 |
|---|---|---|---|
| D3 | 接通 MusicQueueProvenance → `@Published queueProvenance`；电台/AM 流下 Up Next 空态明说「Music 未提供此来源的队列」；全部空态/标题走 L10n（en/zh） | 无 | 完成 e31702c+4dee44c（QueueProvenanceEmptyStateTests 9 条）|
| D1 | 点行跳曲反馈：行级 pending 状态（点击即显、换曲确认或超时/失败清除，假时钟单测）+ hover 手形光标；playTrack(persistentID:) 补失败回调 | 无 | 完成 f778e4c+4dee44c（JumpToPendingStateTests 5 条）|
| D4 | 先出只读审计：漂移场景（外部重排 hash 探不到、Music 退出留陈旧行、慢 SB 读）、旧分支未合入的加固清单、最小修复与测试计划 | 无 | 审计 62d62bf；修复 5ec0238（QueueSyncHardeningTests 14 条：SB 超时不清行、Music 不可用清幽灵行、Recent 扫描 generation 守卫）。后续项：库歌单→电台的上下文失效函数、纯重排 hash 探测 |
| D2 | 列表行 Love：按 persistentID 读/写 favorited（sdef 实查：Music 字典只有 `favorited` code pLov，无 `loved`；现有 toggleStar 写 "loved" 键是失效属性） + 取队列时顺带读状态；形态待裁（建议 hover 心形 + 右键菜单） | 创始人 09-10 转达：列表行只应有一个「调整顺序」按钮，hover 心形/右键菜单存疑，D2 挂起，行上不加任何菜单；「调整顺序」与 08-21 spike（真 Up Next 不可重排）冲突，主会话正在请示；真机验证脚本 scripts/verify_playlist_move_reflects_upnext.sh 备好待创始人自己跑 | 挂起 |
| D5 | 全局快捷键：播放/暂停、上下首、显示/隐藏面板、贴边隐藏；Carbon RegisterEventHotKey 路径不弹权限；是否引 KeyboardShortcuts 待裁；创始人加硬约束：最终上 App Store，先验证沙盒下 Carbon 热键与审核证据、与 apple-events entitlement 的牵制 | 证据调研中 | 待裁 |

## 禁区
PlaylistView：禁 Section+LazyVStack、禁嵌套 ScrollView、禁 .hudWindow、保留 matchedGeometryEffect 与 per-row visualEffect blur。SB 只在 scriptingBridgeQueue；禁 Thread.sleep；慢 SB 读不当换歌信号；不引私有 API。

## 协作
- WT-C：SongRow 整行归 WT-D，WT-C 不碰 PlaylistView.swift；WT-C 会改 MusicMiniPlayerApp.swift（菜单栏点击/窗口出现），D5 接线放独立文件只加调用行。
- WT-E：MusicController 公开签名不变，D2/D4 改动照旧放 MusicController+Playback.swift，WT-E 合并后 rebase。

## 09-11 真机与文档核实
- `move track` 对正在播放的库用户歌单无效（三轮实测，顺序与 index 不变，播放按原序），路线 A 连窄口子也不成立。
- MusicKit：SystemMusicPlayer 平台清单无 macOS；ApplicationMusicPlayer.Queue.entries `{ get set }`（RangeReplaceableCollection）可任意重排，但那是 nanoPod 自己的队列，不影响 Music.app；WWDC21 10294：两种 player 自动上报 Now Playing 并处理 remote command，ApplicationMusicPlayer 时 nanoPod 成为 now-playing app。重赋 entries 是否中断播放、与 Music.app 同开互斥：文档未写。
- 全量测试：1049 个，4 个失败全在 NativeLyricsRenderChurnTests（渲染时序，本分支未触及渲染文件，属预存/环境型）。
