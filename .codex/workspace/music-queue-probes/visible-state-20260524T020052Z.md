# Visible Music.app Queue State

Timestamp: 2026-05-24T02:00:52Z probe run, local capture shortly after.

Context:

- Music.app queue pane was visible via the bottom-right `playing next` button.
- The current playback context was station/URL-track style playback.
- Computer-use accessibility tree showed the play queue container and visible
  rows, while the public AppleScript probe could not read a public queue object
  or `current playlist`.

Visible Music.app queue pane excerpt:

- `play queue` container visible.
- Row: `Jailbird` / `Cass Phang` / `Jailbird`.
- Section: `Continue Playing`.
- Source: `From 李翊楠’s Station`.
- Row: `愛不了多久` / `Sandy Lam` / `Love, Sandy`.
- Row: `留給這世上我最愛的人` / `Joey Yung` / `留給世上最愛羅文的人`.

Matching public probe:

- `.codex/workspace/music-queue-probes/public-surface-20260524T020052Z.txt`
- `current_track.name=為何又是這樣錯`
- `current_track.artist=Sammi Cheng`
- `current_track.class=URL track`
- `current_playlist.error=Music got an error: Can’t get current playlist.`
- `playlists_named_up_next=` empty
- `classification.outcome=unavailable_no_current_playlist`

Conclusion:

For this station/URL-track context, Music.app visibly has a queue, but the
public AppleScript/ScriptingBridge surface tested by the probe does not expose
that queue. This proves the current public `currentPlaylist.tracks` strategy is
not exact for this context.
