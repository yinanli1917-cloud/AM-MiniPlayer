# Build artifacts
.build/
.swiftpm/

# Xcode
xcuserdata/
*.xcworkspace
*.xcodeproj

# macOS
.DS_Store

# Swift Package Manager
Packages/
Package.pins
Package.resolved
.build/

# Sensitive files
config.json
*.env
.env*
secrets/

# Claude Code settings (contains personal paths & server IPs)
.claude/settings.local.json
.claude/scheduled_tasks.lock
.gstack/

# Release artifacts (built on demand)
releases/
.nightcrawl/

# Claude workflow state (hook-enforced, not part of codebase)
.claude/workflow.md
.claude/tasks/

# Codex workflow state (session/task runtime, not project source)
.codex/runtime/
